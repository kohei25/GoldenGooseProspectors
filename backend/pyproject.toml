[tool.poetry]
name = "eggs"
version = "0.1.0"
description = ""
authors = ["niko25"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
